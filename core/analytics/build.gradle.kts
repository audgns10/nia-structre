plugins {
    id("build.android.core")
    id("build.android.hilt")
    id("build.android.compose")
}

android {
    namespace = "com.example.analytics"
}

dependencies {
    // todo: add dependencies(firebase analytics)
}